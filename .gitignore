# deps
node_modules/
.pnp.*
.yarn/cache
.yarn/install-state.gz

# env
.env
.env.local
.env.*.local
!.env.example

# builds
.next/
out/
dist/
build/
artifacts/
cache/
typechain-types/
coverage/

# harness artefacts
.harness/runs/
.harness/checkpoints/

# local data
*.log
.DS_Store
tmp/
packages/hardhat/deployments/localhost/

# CI workflow is kept locally but not pushed: the current GitHub token lacks
# the `workflow` scope. Run `gh auth refresh -s workflow`, then:
#   git add -f .github/workflows/ci.yaml && git commit -m "ci: add workflow"
.github/workflows/
